# Virgo Server with OpenMRS bundles #

To run: "bin/startup.sh" (bin/startup.bat on Windows)

### Note ###
I've increased Java heap and PermGen spaces in startup.sh script. 
Do it for windows version if you have OutOfMemoryError